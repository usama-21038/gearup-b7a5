import { ProfileView } from "@/components/profile-view";

export default function ProviderProfilePage() {
  return <ProfileView note="Profile editing isn't available yet — the GearUp API doesn't expose an update-profile endpoint. Contact support if any of these details need to change." />;
}
